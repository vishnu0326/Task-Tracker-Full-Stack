version: "3.8"

services:
  backend:
    build: ./backend
    container_name: task_backend
    ports:
      - "8080:8080"
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: task_redis
    ports:
      - "6379:6379"